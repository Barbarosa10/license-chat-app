import React from 'react';

// Popup component
const PopupResponse = ({ message }) => {
    return (
      <div className="popup">
        {message}
      </div>
    );
  };
  
export default PopupResponse